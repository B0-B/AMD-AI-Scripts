services:
  app:
    
    # Base environment
    image: docker.io/vllm/vllm-openai-rocm:latest
    env_file:
      - ./amd.env
    
    # ==== Benchmark Arguments (will be ignored by docker) ====
    x-benchmark-args:
      models:
        - "amd/Llama-3.1-8B-Instruct-FP8-KV"
        - "amd/Llama-3.3-70B-Instruct-FP8-KV"
      hf_token: "<hf_token_here>"
      device_type: GPU
      device_name: MI300X
      concurrencies: [1, 4, 8, 16, 32, 64]
      input_lengths: [1024, 4096]
      output_lengths: [1024, 4096]
      dataset_type: random
      num_tokens: 50000
      csv_path: "./amd_vllm_integrated_benchmark_results.csv"
      warmup_runs: 5
    # =========================================================

    # Mount devices
    devices:
      - "/dev/kfd:/dev/kfd"
      - "/dev/dri:/dev/dri"
    group_add:
      - video
    shm_size: '128gb' 
    cap_add:
      - SYS_PTRACE  # Common for ROCm/debugging
    security_opt:
      - seccomp:unconfined  # disables seccomp profiles
    ipc: host  # High-performance computing often needs host IPC
    network_mode: host  # Use 'host' for maximum network performance

    # Mount benchmark files
    volumes:
      - .:/app
    working_dir: /app
    entrypoint: ["/bin/sh", "-c"]
    command: ["python3 -u broad_bench.py > results/benchmark.log 2>&1"]
    # Clean up
    restart: "no"